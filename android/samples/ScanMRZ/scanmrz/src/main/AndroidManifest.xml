<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MRZScanner">
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ResultActivity"
            android:exported="true"
            android:screenOrientation="portrait">
        </activity>

        <!--
        MRZScannerActivity's default screenOrientation is portrait.
        If you want to customize orientation,
        redeclare the same Activity in your app manifest and override `android:screenOrientation`.
        -->
<!--        <activity-->
<!--            android:name="com.dynamsoft.mrzscannerbundle.ui.MRZScannerActivity"-->
<!--            android:screenOrientation="fullSensor"-->
<!--            tools:replace="android:screenOrientation"/>-->
    </application>

</manifest>